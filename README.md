# 🐕 Corgi Design

AI驱动的网页设计助手 - 智能分析网页结构，一键生成设计建议与优化方案

## ✨ 功能特性

- 🔍 **智能分析** - 自动分析网页结构、标题层级、图片和链接数量
- 💡 **设计建议** - 基于最佳实践提供SEO和性能优化建议
- 🎨 **现代UI** - 采用深色主题的侧边栏设计，美观不干扰
- ⚡ **轻量快速** - React + Vite 构建，体积小加载快

## 🛠️ 技术栈

- **React 18** - 用户界面
- **Vite 5** - 构建工具
- **Tailwind CSS** - 样式框架
- **Chrome Extension Manifest V3** - 最新扩展API

## 📁 项目结构

```
corgi/
├── dist/                    # 构建输出（Chrome加载此目录）
│   ├── background/
│   ├── content/
│   ├── icons/
│   ├── popup/
│   └── manifest.json
├── src/
│   ├── popup/              # Popup弹窗
│   │   ├── index.html
│   │   ├── main.jsx
│   │   └── Popup.jsx
│   ├── content/            # 内容脚本（侧边栏）
│   │   ├── content.jsx
│   │   └── content.css
│   ├── background/         # 后台脚本
│   │   └── background.js
│   └── index.css           # 全局样式
├── public/                 # 静态资源
│   ├── icons/
│   └── manifest.json
├── package.json
├── vite.config.js          # Popup构建配置
├── vite.content.config.js  # Content Script构建配置
├── tailwind.config.js
└── postcss.config.js
```

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm >= 8.0.0

### 安装依赖

```bash
npm install
```

### 开发构建

```bash
npm run build
```

### 加载扩展

1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 开启右上角「开发者模式」
4. 点击「加载已解压的扩展程序」
5. 选择项目的 `dist` 目录

## 📖 使用方法

1. 打开任意网页
2. 点击浏览器工具栏中的 🐕 Corgi Design 图标
3. 点击「开始分析」按钮
4. 右侧将弹出分析侧边栏，显示页面信息和设计建议

## 🔧 开发命令

| 命令 | 说明 |
|------|------|
| `npm run build` | 构建生产版本 |
| `npm run build:popup` | 仅构建Popup |
| `npm run build:content` | 仅构建Content Script |
| `npm run dev` | 开发模式（仅Popup热更新） |

## 📝 开发指南

### 修改 Popup

编辑 `src/popup/Popup.jsx`，使用 Tailwind CSS 类名进行样式设置。

### 修改侧边栏

编辑 `src/content/content.jsx`，侧边栏使用内联样式以确保与页面样式隔离。

### 添加新权限

在 `public/manifest.json` 中添加所需权限，然后重新构建。

## 🎨 自定义主题

在 `tailwind.config.js` 中可以自定义颜色主题：

```javascript
theme: {
  extend: {
    colors: {
      primary: { /* 主色调 */ },
      secondary: { /* 次要色 */ },
      accent: { /* 强调色 */ }
    }
  }
}
```

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
